--Name : ActivityFrame6View.lua
--Author : generated by FairyGUI
--Date : 2020-5-9
--Desc : added by xhd UI框架模态4

local ActivityFrameViewBase = require "Game.Modules.ActivityFrame.ActivityFrameViewBase"
local ActivityFrame7View,Super = class("ActivityFrame6View", ActivityFrameViewBase)
local ActivityMap = require "Game.Modules.Activity.ActivityMap"
function ActivityFrame7View:ctor()
	self._packName = "ActivityFrame"
	self._compName = "ActivityFrame7View"
end

function ActivityFrame7View:_initEvent( )
end

function ActivityFrame7View:close_ActivityView( actId )
    ViewManager.close("ActivityFrame7View")
	local winData = ActivityModel:marketUIWinData(actId) 
	if not winData then
		return
	end
    if (winData and (#winData > 0)) then
        ViewManager.open(self._viewName,{viewData =winData,page = winData[1].page})
    else
        RollTips.show(Desc.activity_txt1);
    end
end

return ActivityFrame7View