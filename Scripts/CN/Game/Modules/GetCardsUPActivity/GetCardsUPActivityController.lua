--Name : GetCardsUPActivityController.lua
--Author : generated by FairyGUI
--Date : 2020-9-3
--Desc : UP探员活动招募控制器

local GetCardsUPActivityController = class("GetCardsUPActivityController",Controller)

function GetCardsUPActivityController:init()
	
end

function GetCardsUPActivityController:Activity_UpdateData( _,params )
	if params.type == GameDef.ActivityType.UpHero then
		printTable(1,"GetCardsUPActivityController 活动数据更新 Activity_UpdateData",params)
		local data = ActivityModel:getActityByType( params.type )
		if not data then
			return
		end

		if params.fromLogin then --如果是初始化的数据
		end

		if params.endState then --如果是true 直接结束
			Dispatcher.dispatchEvent("close_ActivityView",3) 
			return
		end
	    GetCardsUPActivityModel:initData(params)
	end


end

return GetCardsUPActivityController