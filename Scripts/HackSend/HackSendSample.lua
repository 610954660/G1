return {
	-- ["Scene_Move"] ={
		--这个例子就是将所有points的x坐标都变成10
		-- ["points.*.x"] = 1100,
		--这个例子意思就是以数字1做key改变对应的table的x变成1
		-- ["points.$1.x"] = 10,
		-- --这个例子意思就是以数字1做key改变对应的table变成下面的值
		-- ["points.$1"] = {x = 10, y = 10},

		-- --points里面全部的内容都变成对应的值
		-- ["points.*"] = {x = 10, y = 10},

		--给这个接口凭空加多一个值（如果有值的情况下直接改对应的值）
		-- ["testAddValue"] = 100,
	-- },
	-- ["Scene_Pass"] = {
	-- 	x = 10,
	-- 	y = 10,
	-- 	mapId = 0,
	-- }
}